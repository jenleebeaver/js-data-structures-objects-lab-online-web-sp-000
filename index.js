// Write your solution in this file!

const Driver = {};

function
updateDriverWithKeyAndValue(Driver, key, value){
  const newDriver = { ...obj };

  newDriver[key] = value;

  return newDriver;
}
const Driver = updateDriverWithKeyAndValue(newDriver, key, value){};
